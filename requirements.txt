# Core data processing
pandas>=2.0.0,<3.0.0
python-dateutil>=2.8.2

# Cleaning and validation
phonenumbers>=8.13.0
frictionless>=5.0.0,<6.0.0

# Encoding detection
chardet>=5.0.0

# Excel file support
openpyxl>=3.1.0
# Legacy binary .xls (BIFF). xlrd>=2 reads .xls only, which is exactly the gap
# openpyxl leaves; without it a .xls upload fails with a clear install hint.
xlrd>=2.0.1
# Writes the legacy .xls fixtures the ingestion tests read back. Test-only, but
# listed here so the suite runs from this file alone; the tests skip if absent.
xlwt>=1.3.0

# Runs the NiceGUI end-to-end tests (tests/integration/test_ui_end_to_end.py),
# which render real pages through NiceGUI's async test fixtures. Test-only.
pytest-asyncio>=0.24

# PDF bank statement extraction (ingestion layer)
pdfplumber>=0.11.0

# Mapping configuration
PyYAML>=6.0

# Reporting
Jinja2>=3.1.2
# PDF export (utility 8: "export to any format"). Pure Python, no system libs.
reportlab>=4.0.0

# S3 connector (distribution layer). Optional at runtime: the connector raises a
# clear "pip install boto3" message, but it is listed so an S3 user is not
# surprised. The other four connectors use plain HTTP and need nothing extra.
boto3>=1.34.0

# Web framework - NiceGUI (the DataReady interface)
nicegui>=3.0.0,<4.0.0

# REST API (distribution layer) - FastAPI serves the /validate /clean /profile
# /reconcile endpoints, with uvicorn as the server and multipart for uploads.
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
