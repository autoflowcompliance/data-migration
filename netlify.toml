[build]
  command = "npx @stlite/cli web data_migration_tool --requirements data_migration_tool/requirements.txt"
  publish = "build"

# This site is a static stlite (in-browser Streamlit) build; no Netlify
# Functions are used. The `@stlite/cli web` command outputs into `./build`.